# UnionVisualizer

Visualization components for Union UI.

## Installation

Add the package to your `Package.swift`:

```swift
dependencies: [
    .package(url: "https://github.com/unionst/union-visualizer.git", .upToNextMajor(from: "1.0.0"))
]
```

## Usage

```swift
import UnionVisualizer
```
